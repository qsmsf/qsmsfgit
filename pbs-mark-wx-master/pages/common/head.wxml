<template name="head">
  <view class="page-head">
    <view class="page-head-title">{{title}}</view>
    <view class="page-head-line"></view>
    <view wx:if="{{content}}" class="page-head-desc">{{desc}}</view>
  </view>
</template>
